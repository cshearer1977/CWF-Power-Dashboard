# Copy to .env and fill in your keys. .env is git-ignored.
# Ember API key — request one at https://ember-energy.org/data/data-tools/api/
EMBER_API_KEY=your-ember-api-key-here

# Climate TRACE does not require a key; this var is optional and reserved for future use.
# CLIMATE_TRACE_API_KEY=
